# Use local image
FROM minituff/nautical-backup:test

RUN \
    echo "**** Installing pacakges ****" && \
    apk add --no-cache \
    ruby-full \
    py3-pip \
    nano && \
    echo "**** Installing ruby pacakges (for tests) ****" && \
    gem install bashcov simplecov-cobertura simplecov-html

VOLUME [ "/app" ]
VOLUME [ "/tests" ]